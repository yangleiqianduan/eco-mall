@import "~common/global.styl"

.wrap
  box()
  .tab
    flex 1
    text-align center
    margin px2rem(10) 0
    a
      color $color-normal
      font-size $font-size14
  .active
    border(left,3px,$color-dark)
    a
      color $color-dark
      font-size $font-size16
      font-weight bold
.vertical
  flex-direction column
